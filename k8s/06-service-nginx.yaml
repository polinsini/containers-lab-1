apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
    lab: lab2
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort